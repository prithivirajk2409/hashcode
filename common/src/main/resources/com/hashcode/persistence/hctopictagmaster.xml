<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hashcode.persistence.HCTopicTagMaster">
	<select id="getTopicTagsForProblem" parameterType="java.util.Map" resultType="HCTopicTagMaster">
	<![CDATA[ 
		SELECT
			*
		FROM
			ndtopictagmaster
		WHERE
			problem_id = #{problemId}
	]]>
	</select>
</mapper>